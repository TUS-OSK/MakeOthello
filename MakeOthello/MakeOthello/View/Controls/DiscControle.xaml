<UserControl
    x:Class="MakeOthello.View.Controls.DiscControle"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MakeOthello.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="60"
    d:DesignWidth="60"
    Background="Transparent">
    <Grid>
        <Border CornerRadius="7" Margin="1" Background="{StaticResource ThemeColorDark}"></Border>
            <Ellipse  Height="{Binding Height}" Width="{Binding Width}"             
                 Fill="{Binding DiscColorBrush}" 
                 Opacity="{Binding DiscOpacity}"/>
        <Button Background="Transparent" Command="{Binding DiscTapedCommand}"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></Button>
    </Grid>
</UserControl>
